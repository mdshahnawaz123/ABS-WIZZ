<Window x:Class="ABS_WIZZ.UI.LoginWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ABS_WIZZ.UI"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300" Title=" Sign In" SizeToContent="WidthAndHeight" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Username" />
            <TextBox x:Name="TB_Username" Width="260" Margin="0,4,0,8"/>
            <TextBlock Text="Password" />
            <PasswordBox x:Name="PB_Password" Width="260" Margin="0,4,0,8"/>
        </StackPanel>

        <TextBlock x:Name="TB_Error" Foreground="Red" Grid.Row="1" Margin="0,8,0,0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Sign in" Width="80" Click="SignIn_Click"/>
        </StackPanel>
    </Grid>
</Window>
